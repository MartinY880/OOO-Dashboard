# Appwrite Configuration (Client-side - publicly accessible)
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your_project_id

# Appwrite Configuration (Server-side - keep secret)
APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=your_project_id
APPWRITE_API_KEY=your_api_key

# Encryption (Server-side - keep secret)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY_32B_BASE64=your_32_byte_base64_encoded_key

# n8n Webhook (Optional - for automation integration)
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/endpoint
N8N_SIGNATURE_SECRET=your_webhook_secret

# Note: With Appwrite OAuth, you don't need these anymore:
# - NEXTAUTH_URL
# - NEXTAUTH_SECRET
# - AZURE_CLIENT_ID (configured in Appwrite Console)
# - AZURE_CLIENT_SECRET (configured in Appwrite Console)
# - AZURE_TENANT_ID (configured in Appwrite Console)
